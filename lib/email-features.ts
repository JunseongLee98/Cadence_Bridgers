/**
 * Email verification + notification delivery (Resend).
 * Disabled until RESEND_API_KEY and production auth are configured.
 * Set to `true` to turn the routes and client hooks back on.
 */
export const EMAIL_FEATURES_ENABLED = false;
